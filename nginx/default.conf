upstream dashboard {
    server web:8000;
}

server {
    listen 80;
    server_name jkleinlabs.com;
    
    # Redirect all HTTP requests to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
    
    # Allow certbot to authenticate domains
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Static test file
    location /csrf-test.html {
        root /usr/share/nginx/html;
        add_header Content-Type text/html;
    }
}

server {
    listen 443 ssl;
    server_name jkleinlabs.com;
    
    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/jkleinlabs.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jkleinlabs.com/privkey.pem;
    
    # Enable this for local testing
    #ssl_verify_client off;
    
    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;
    
    # HSTS (optional, but recommended)
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    
    # Static CSRF test page
    location = /csrf-test.html {
        root /usr/share/nginx/html;
        add_header Content-Type text/html;
    }
    
    # Proxy settings
    location / {
        proxy_pass http://dashboard;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_redirect off;
        
        # Cookie and CSRF headers
        proxy_set_header Cookie $http_cookie;
        proxy_set_header X-CSRFToken $http_x_csrftoken;
        
        # Debug headers
        add_header X-Debug-Request-Host $host always;
        add_header X-Debug-Forwarded-Proto $scheme always;
    }

    location /static/ {
        alias /usr/share/nginx/html/static/;
    }
}
